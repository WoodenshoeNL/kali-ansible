
- name: obsidian - install package
  ansible.builtin.apt:
    name: obsidian
    state: latest      # always pull the newest version available
    update_cache: yes
    cache_valid_time: 3600
  when: vmware_env == "workstation"