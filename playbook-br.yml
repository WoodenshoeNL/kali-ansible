# Beads Rust (br) - Standalone playbook (Extra role, beads_rust tasks only)
# https://github.com/Dicklesworthstone/beads_rust
#
# Run: ansible-playbook -i localhost, -c local playbook-br.yml --tags beads_rust

- hosts: all
  gather_facts: yes
  become: true

  roles:
    - Extra
